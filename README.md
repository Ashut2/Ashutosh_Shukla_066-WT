# Assignment-01
Scenario_1

## Overview ✅
A small sample workspace containing example files in multiple languages (HTML, C++, Java, Python) and accompanying notes.

## Files 🔧
- `index.html` — Simple HTML page (open in a browser).
- `main.cpp` — C++ implementation of the Two Sum algorithm (O(n)). Compile: `g++ main.cpp -o main` and run: `./main` or `.\main` on Windows.
- `Main.java` — Java program to find the second largest element without sorting. Compile: `javac Main.java` and run: `java Main`.
- `script.py` — Python script that prints a message. Run: `python script.py`.
- `notes.txt` — Plain-text notes about the project.
- `info.txt` — Descriptions of files in this folder.
- `.git/` — Git metadata (hidden folder for version control).

## Notes on line endings ⚠️
On Windows Git may convert LF to CRLF in the working copy. To control behavior, set `core.autocrlf` or add a `.gitattributes` file (e.g., `*.txt text eol=lf`).

## Contact / Author ✉️
Created on 2026-02-02.
